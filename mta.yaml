_schema-version: "3.2"
ID: employeestore
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: employeestore-destination-content
  type: com.sap.application.content
  requires:
  - name: employeestore-destination-service
    parameters:
      content-target: true
  - name: employeestore-repo-host
    parameters:
      service-key:
        name: employeestore-repo-host-key
  - name: employeestore-uaa
    parameters:
      service-key:
        name: employeestore-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: employeestore_html_repo_host
          ServiceInstanceName: employeestore-html5-srv
          ServiceKeyName: employeestore-repo-host-key
          sap.cloud.service: employeestore
        - Authentication: OAuth2UserTokenExchange
          Name: employeestore_uaa
          ServiceInstanceName: employeestore-xsuaa-srv
          ServiceKeyName: employeestore-uaa-key
          sap.cloud.service: employeestore
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: employeestore-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: employeestore-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - employeestore.zip
      name: employeestore
      target-path: resources/
- name: employeestore
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: employeestore-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: employeestore-destination-service
    service-plan: lite
- name: employeestore-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: employeestore-xsuaa-srv
    service-plan: application
- name: employeestore-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: employeestore-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
